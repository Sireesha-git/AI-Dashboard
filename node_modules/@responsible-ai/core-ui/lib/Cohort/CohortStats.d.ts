export declare class BaseCohortStats {
    totalAll: number;
    totalCohort: number;
    errorCoverage: number;
    constructor(totalCount: number, baseTotalCount: number, errorCoverage: number);
}
/**
 * Contains basic cohort metric statistics.
 */
export declare class MetricCohortStats extends BaseCohortStats {
    metricValue: number;
    metricName: string;
    constructor(totalCount: number, baseTotalCount: number, metricValue: number, metricName: string, errorCoverage: number);
}
/**
 * Contains additional error information for classification scenario
 * such as the number of correct or incorrect instances.
 */
export declare class ErrorCohortStats extends MetricCohortStats {
    totalCorrect: number;
    totalCohortCorrect: number;
    totalIncorrect: number;
    totalCohortIncorrect: number;
    constructor(numError: number, totalCount: number, baseNumError: number, baseTotalCount: number, metricValue: number, metricName: string);
}
