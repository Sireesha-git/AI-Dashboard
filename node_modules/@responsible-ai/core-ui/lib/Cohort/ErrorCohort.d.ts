import { IFilter } from "../Interfaces/IFilter";
import { JointDataset } from "../util/JointDataset";
import { Cohort } from "./Cohort";
import { MetricCohortStats } from "./CohortStats";
import { CohortSource } from "./Constants";
export declare class ErrorCohort {
    cohort: Cohort;
    jointDataset: JointDataset;
    cells: number;
    source: CohortSource;
    isTemporary: boolean;
    isAllDataCohort: boolean;
    cohortStats: MetricCohortStats;
    metricValue: number;
    metricName: string;
    constructor(cohort: Cohort, jointDataset: JointDataset, cells?: number, source?: CohortSource, isTemporary?: boolean, cohortStats?: MetricCohortStats | undefined, isAllDataCohort?: boolean);
    static getDataFilters(filters: IFilter[], features: string[]): IFilter[];
    filtersToString(): string[];
    private updateStatsFromData;
}
