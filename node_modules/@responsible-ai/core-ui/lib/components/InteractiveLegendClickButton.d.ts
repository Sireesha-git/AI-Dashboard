import React from "react";
interface IInteractiveLegendProps {
    activated: boolean;
    index: number;
    name: string;
    colorBoxClassName?: string;
    onClick?: (index: number) => void;
}
export declare class InteractiveLegendClickButton extends React.PureComponent<IInteractiveLegendProps> {
    private readonly classes;
    render(): React.ReactNode;
    private onClick;
}
export {};
