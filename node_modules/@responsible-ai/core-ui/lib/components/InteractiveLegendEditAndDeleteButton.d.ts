import React from "react";
interface IInteractiveLegendProps {
    index: number;
    onDelete?: (index: number) => void;
    onEdit?: (index: number) => void;
}
export declare class InteractiveLegendEditAndDeleteButton extends React.PureComponent<IInteractiveLegendProps> {
    private readonly classes;
    render(): React.ReactNode;
    private onEdit;
    private onDelete;
}
export {};
