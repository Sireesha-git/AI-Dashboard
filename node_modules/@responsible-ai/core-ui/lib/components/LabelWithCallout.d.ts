import { DirectionalHint } from "@fluentui/react";
import React from "react";
import { ITelemetryEvent } from "../util/ITelemetryEvent";
import { TelemetryEventName } from "../util/TelemetryEventName";
export interface ILabelWithCalloutProps {
    label: string;
    calloutTitle: string | undefined;
    calloutEventName?: TelemetryEventName;
    renderOnNewLayer?: boolean;
    type?: "label" | "button";
    telemetryHook?: (message: ITelemetryEvent) => void;
    iconButtonId?: string;
    calloutTarget?: string;
    directionalHint?: DirectionalHint;
}
interface ILabelWithCalloutState {
    showCallout: boolean;
}
export declare class LabelWithCallout extends React.Component<ILabelWithCalloutProps, ILabelWithCalloutState> {
    constructor(props: ILabelWithCalloutProps);
    render(): React.ReactNode;
    private toggleCallout;
}
export {};
