import { IColumn } from "@fluentui/react";
import { JointDataset } from "../util/JointDataset";
export interface ITableState {
    rows: any[];
    columns: IColumn[];
}
export declare function areRowPredTrueLabelsEqual(row: {
    [key: string]: number;
}, jointDataset: JointDataset): boolean;
export declare function constructRows(cohortData: Array<{
    [key: string]: number;
}>, jointDataset: JointDataset, viewedRows: number, filterFunction?: (row: {
    [key: string]: number;
}) => boolean, indexes?: number[], colors?: string[]): any[];
export declare function rowsFromCustomPoints(jointDataset: JointDataset, customPoints: Array<{
    [key: string]: any;
}>, viewedRows: number): any[];
export declare function constructCols(viewedCols: number, featureNames: string[], jointDataset: JointDataset, isCustomPointsView: boolean, hasColors?: boolean): IColumn[];
