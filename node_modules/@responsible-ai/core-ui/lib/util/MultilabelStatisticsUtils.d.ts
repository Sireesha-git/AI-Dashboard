import { ILabeledStatistic } from "../Interfaces/IStatistic";
import { JointDataset } from "./JointDataset";
export declare enum MultilabelMetrics {
    ExactMatchRatio = "exactMatchRatio",
    HammingScore = "hammingScore"
}
export declare const generateMultilabelStats: (jointDataset: JointDataset, selectionIndexes: number[][]) => ILabeledStatistic[][];
