import { ModelTypes } from "../Interfaces/IExplanationContext";
import { ILabeledStatistic } from "../Interfaces/IStatistic";
import { JointDataset } from "./JointDataset";
declare type QuestionAnsweringCacheType = Map<string, [
    number,
    number,
    number,
    number,
    number,
    number
]>;
export declare const generateMetrics: (jointDataset: JointDataset, selectionIndexes: number[][], modelType: ModelTypes, objectDetectionCache?: Map<string, [number, number, number]>, objectDetectionInputs?: [string, string, number], questionAnsweringCache?: QuestionAnsweringCacheType) => ILabeledStatistic[][];
export {};
