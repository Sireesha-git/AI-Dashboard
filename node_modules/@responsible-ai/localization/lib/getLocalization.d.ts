import { LocalizedStrings } from "localized-strings";
import { Language } from "./Language";
declare type NestedPartial<T> = {
    [P in keyof T]?: T[P] | NestedPartial<T[P]>;
};
export declare type ILocalizationConfig<T> = {
    en: T;
} & {
    readonly [key in Language]: NestedPartial<T>;
};
export declare type ILocalization<T> = Omit<LocalizedStrings<T>, "formatString"> & {
    formatString(str: string, ...values: Array<string | number | undefined>): string;
};
export declare function getLocalization<T>(lang: ILocalizationConfig<T>): ILocalization<T>;
export {};
