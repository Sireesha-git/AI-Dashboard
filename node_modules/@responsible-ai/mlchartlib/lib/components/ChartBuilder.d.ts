import { IData } from "./IData";
export declare class ChartBuilder {
    static buildPlotlySeries<T>(datum: IData, rows: T[]): IData[];
    private static getHasVectors;
    private static buildDefaultSeries;
}
