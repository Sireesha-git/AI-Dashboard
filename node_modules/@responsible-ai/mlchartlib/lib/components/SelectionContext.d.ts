export interface ISelectionContextSubscriptions {
    selectionCallback: (s: string[]) => void;
    hoverCallback?: (s: string | undefined) => void;
    listenerCountCallback?: (n: number) => void;
}
export declare class SelectionContext {
    selectedIds: string[];
    hoveredId: string | undefined;
    listenerCount: number;
    readonly propertyOfInterest: string;
    private selectionSubs;
    private hoverSubs;
    private countSubs;
    private maxItems;
    constructor(propertyOfInterest: string, maxItems?: number);
    onSelect(newSelections: string[]): void;
    onHover(newItem: string | undefined): void;
    subscribe(subs: ISelectionContextSubscriptions): string;
    unsubscribe(id: string): void;
}
